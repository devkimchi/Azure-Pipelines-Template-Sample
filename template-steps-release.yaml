parameters:
  message: ''

steps:
- task: CmdLine@2
  displayName: 'Echo Greeting in CommandLine'
  inputs:
    script: |
      Echo $(Greeting)
      Echo ${{ parameters.message }}
- task: PowerShell@2
  displayName: 'Echo Greeting in PowerShell'
  inputs:
    targetType: Inline
    script: |
      Write-Host "$(Greeting)"
      Write-Host "${{ parameters.message }}"
